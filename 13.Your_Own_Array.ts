const transprotation :string[] = [ 'motorcycle', 'car', 'train', 'cycle' , 'NINJA H2R'];
for( let i=0;i<transprotation.length; i++ ){
    console.log('I would like to own a ' + transprotation[i])
}