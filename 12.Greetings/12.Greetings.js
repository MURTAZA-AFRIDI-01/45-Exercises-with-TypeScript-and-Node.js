"use strict";
const names = ['komi', 'abdullah', 'asfand', 'khizar' , 'hamza tayyab' , 'rana tayyab'];
const message = 'wellcome to class buddy: ';
for (let i = 0; i < names.length; i++) {
    console.log(message + names[i]);
}
